server:
  port: 8888

spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: C:/projects/spring-cloud-msa-labs-v1/backend/config-repo
#        git:
#          uri: file:///${user.dir}/../config-repo
